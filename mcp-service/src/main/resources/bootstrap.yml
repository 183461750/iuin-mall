spring:
  profiles:
    active: local
#    active: temp2
    include: nacos,base-${spring.profiles.active}
#    include: common
  application:
    name: mcp-service
  main:
    allow-bean-definition-overriding: true

#  config:
#    import: optional:classpath:bootstrap-base-local.yml
