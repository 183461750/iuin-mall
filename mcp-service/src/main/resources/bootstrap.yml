spring:
  profiles:
    active: local
#    active: temp2
    include: base-${spring.profiles.active},jpa,nacos,common
#    include: common
  application:
    name: mcp-service
  main:
    allow-bean-definition-overriding: true

#  config:
#    import: optional:classpath:bootstrap-base-local.yml
