# 使用说明文档

## 导入nacos

[nacos3.0](http://10.0.16.146:8080)
    console被分离, 访问web后台页面地址端口由原来的8848变为8080

导入配置

进入`nacos_config`目录, 选中`.metadata.yml`文件以及`.metadata.yml`文件中的`group`配置值对应的目录
    例如: `group: v1`, 则需选中`v1`目录和`.metadata.yml`文件
