spring:
  profiles:
    active: local
#    active: temp2
    include: common,common-${spring.profiles.active}
#    include: common
  application:
    name: agent-service
  main:
    allow-bean-definition-overriding: true

#  config:
#    import: optional:classpath:bootstrap-common-local.yml
